services:
  typesense-test:
    image: typesense/typesense:29.1
    ports:
      - "8109:8108"
    volumes:
      - typesense-test-data:/data
    command: >
      --data-dir /data
      --api-key test-key
      --enable-cors

volumes:
  typesense-test-data:
